Suggest 5 short URL slugs for a link shortener.

Rules: lowercase letters, numbers, and hyphens only, max 20 chars,
memorable, related to the URL/context below. Do not just reuse the
domain name as the slug - that's not a useful shortening.

URL: {url}
Context: {context}

Return exactly 5 slugs, one per line, nothing else - no numbering, no
bullets, no quotes, no explanation before or after.